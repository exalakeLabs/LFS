Checking Your Usage

You can check current LFS usage in your repository settings:

Go to your repo → Settings → Git LFS

It shows:

Current usage

Who owns the LFS quota (you or your org)

Option to buy more data packs

Or via CLI:

git lfs fetch --all

git lfs ls-files
